---
import Base from '../../layouts/Base.astro';
const title = "Super Admins";
const description = `Govern access, permissions, and data quality across your entire organization.`;
---

<Base title={title} description={description} activeSection="solutions">
<style>

  .nav-item > a:hover, .nav-item.active > a { color:#fff; }
  .nav-item.has-drop > a::after { content:' ▾'; font-size:0.65rem; }
  
  
  .nav-dropdown a:last-child { border-bottom:none; }
  .nav-dropdown a:hover { background:rgba(232,95,23,0.1); color:#fff; padding-left:22px; }
  
  .nav-cta:hover { background:var(--orange-bright) !important; }

.page-hero::before {
    content:''; position:absolute; right:0; top:0; bottom:0; width:40%;
    background:linear-gradient(90deg, transparent, rgba(232,95,23,0.04));
    pointer-events:none;
  }
  .page-hero-eyebrow {
    display:inline-flex; align-items:center; gap:8px;
    font-family:'JetBrains Mono',monospace; font-size:0.65rem;
    letter-spacing:0.2em; text-transform:uppercase; color:var(--orange);
    margin-bottom:20px;
  }
  .page-hero-eyebrow::before { content:''; display:block; width:28px; height:2px; background:var(--orange); }
  .page-hero h1 {
    font-family:'Barlow Condensed',sans-serif; font-weight:900;
    font-size:clamp(3rem,6vw,5.5rem); line-height:0.92;
    letter-spacing:-0.01em; color:#fff; text-transform:uppercase;
    max-width:700px; margin-bottom:22px;
  }
  .page-hero h1 span { color:var(--orange); }
  .page-hero p {
    font-size:1.1rem; line-height:1.7;
    color:rgba(255,255,255,0.55); max-width:560px;
  }
  .page-hero-actions { display:flex; gap:14px; margin-top:34px; flex-wrap:wrap; }

  /* ── SECTION COMMON ── */
  section { padding:88px 56px; }
  
  .eyebrow::after { content:''; display:block; height:1px; width:36px; background:var(--orange); }
  h2 {
    font-family:'Barlow Condensed',sans-serif; font-weight:800;
    font-size:clamp(2.4rem,4vw,3.8rem); line-height:0.95;
    letter-spacing:-0.01em; text-transform:uppercase;
    color:var(--navy); margin-bottom:14px;
  }

.btn-primary:hover { background:var(--orange-bright); transform:translateY(-2px); box-shadow:0 8px 24px rgba(232,95,23,0.4); }
  
  .btn-outline:hover { border-color:var(--orange); color:var(--orange); }
  .btn-navy {
    background:var(--navy); color:#fff; padding:14px 34px;
    border-radius:2px; font-size:0.9rem; font-weight:600;
    text-decoration:none; letter-spacing:0.06em; text-transform:uppercase;
    transition:all 0.2s; display:inline-block;
  }
  .btn-navy:hover { background:var(--navy-deep); transform:translateY(-2px); }

.feat-grid-2 { grid-template-columns:repeat(2,1fr); }
  
  .feat-tile:hover { background:#fff; }
  .feat-tile::after { content:''; position:absolute; bottom:0; left:0; right:0; height:3px; background:var(--orange); transform:scaleX(0); transform-origin:left; transition:transform 0.35s ease; }
  .feat-tile:hover::after { transform:scaleX(1); }
  .feat-tag { font-family:'JetBrains Mono',monospace; font-size:0.6rem; letter-spacing:0.15em; text-transform:uppercase; color:var(--orange); margin-bottom:12px; }
  .feat-tile h3 { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:1.4rem; letter-spacing:0.04em; text-transform:uppercase; color:var(--navy); margin-bottom:8px; }
  .feat-tile p { font-size:0.87rem; line-height:1.7; color:var(--ink-soft); }

  /* ── STEP LIST ── */
  .steps { display:flex; flex-direction:column; gap:0; }
  .step { display:grid; grid-template-columns:60px 1fr; gap:28px; padding:36px 0; border-bottom:1px solid var(--rule); align-items:start; }
  .step:last-child { border-bottom:none; }
  .step-num { font-family:'Barlow Condensed',sans-serif; font-weight:900; font-size:3rem; color:rgba(26,26,26,0.1); line-height:1; text-align:right; }
  .step h3 { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:1.5rem; letter-spacing:0.04em; text-transform:uppercase; color:var(--navy); margin-bottom:8px; }
  .step p { font-size:0.88rem; line-height:1.7; color:var(--ink-soft); }

.check-list li { display:flex; gap:12px; align-items:flex-start; font-size:0.9rem; line-height:1.6; color:var(--ink-mid); }
  .check-list li::before { content:'✓'; color:var(--teal); font-weight:700; flex-shrink:0; margin-top:1px; }

  /* ── ROLE CARDS ── */
  .role-card { background:#fff; border:1px solid var(--rule); border-radius:6px; padding:32px 28px; border-top:3px solid var(--orange); }
  .role-label { font-family:'JetBrains Mono',monospace; font-size:0.6rem; letter-spacing:0.15em; text-transform:uppercase; color:var(--orange); margin-bottom:8px; }
  .role-card h3 { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:1.5rem; letter-spacing:0.04em; text-transform:uppercase; color:var(--navy); margin-bottom:10px; }
  .role-card p { font-size:0.86rem; line-height:1.7; color:var(--ink-soft); }

.cta-strip h2 { color:#fff; margin-bottom:10px; }
  .cta-strip p { color:rgba(255,255,255,0.8); font-size:1rem; max-width:480px; margin:0 auto 28px; line-height:1.65; }
  
  .btn-white:hover { background:var(--navy-deep); color:#fff; transform:translateY(-2px); }

.footer-brand p { font-size:0.84rem; color:rgba(255,255,255,0.4); line-height:1.7; margin-top:12px; max-width:250px; }
  .footer-col h4 { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:0.9rem; letter-spacing:0.12em; text-transform:uppercase; color:var(--orange); margin-bottom:14px; }
  .footer-col ul { list-style:none; display:flex; flex-direction:column; gap:8px; }
  
  .footer-col a:hover { color:#fff; }
  
  .footer-bottom a { color:rgba(255,255,255,0.35); text-decoration:none; }
  .footer-bottom a:hover { color:var(--orange); }

  .fade-up.on { opacity:1; transform:none; }

.footer-brand p { font-size:0.84rem; color:rgba(255,255,255,0.4); line-height:1.7; margin-top:12px; max-width:250px; }
  .footer-col h4 { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:0.9rem; letter-spacing:0.12em; text-transform:uppercase; color:var(--orange); margin-bottom:14px; }
  .footer-col ul { list-style:none; display:flex; flex-direction:column; gap:8px; }
  
  .footer-col a:hover { color:#fff; }
  
  .footer-bottom a { color:rgba(255,255,255,0.35); text-decoration:none; }
  .footer-bottom a:hover { color:var(--orange); }
</style>
<div class="page-hero">
  <div class="page-hero-eyebrow">Solutions</div>
  <h1>Built for <span>Super Admins</span></h1>
  <p>Full platform governance. Configure permissions, manage all users, oversee every game lifecycle, and keep the entire system running clean.</p>
  <div class="page-hero-actions">
    <a href="/contact/" class="btn-primary">Book a Demo</a>
    <a href="/resources/user-guide/" class="btn-outline">View User Guide</a>
  </div>
</div>

<section>
  <div class="eyebrow">Platform Control</div>
  <h2>Every Power. Every<br>Permission. One Role.</h2>
  <p class="section-sub">Super Admins have unrestricted access across all schools, leagues, and sports. You set the rules for everyone else.</p>
  <div class="feat-grid fade-up">
    <div class="feat-tile">
      <div class="feat-tag">Governance</div>
      <h3>Permission Management</h3>
      <p>Configure what each role can see and do. Toggle specific actions on or off per role — from bulk team creation to user management. No dev required.</p>
    </div>
    <div class="feat-tile">
      <div class="feat-tag">Users</div>
      <h3>User Administration</h3>
      <p>Create, edit, deactivate, and reassign user accounts across the entire platform. Manage role assignments and access levels from one admin panel.</p>
    </div>
    <div class="feat-tile">
      <div class="feat-tag">Structure</div>
      <h3>Schools, Leagues &amp; Sports</h3>
      <p>Create and edit schools, configure league structures, define sports and levels. You own the data model — and can change it at any time.</p>
    </div>
    <div class="feat-tile">
      <div class="feat-tag">Data Integrity</div>
      <h3>Game Lifecycle Oversight</h3>
      <p>View, edit, or clean up any game across any school or league. Handle edge cases, duplicate records, and data cleanup without restriction.</p>
    </div>
    <div class="feat-tile">
      <div class="feat-tag">Imports &amp; Exports</div>
      <h3>Schedule Import &amp; Validation</h3>
      <p>Import schedules from CSV with field mapping and validation. Review errors before committing. Export full data snapshots at any time.</p>
    </div>
    <div class="feat-tile">
      <div class="feat-tag">Reporting</div>
      <h3>System-Wide Reports</h3>
      <p>Run reports across every league, school, and sport simultaneously. Generate compliance summaries, season snapshots, and audit logs on demand.</p>
    </div>
  </div>
</section>

<section style="background:var(--bg-warm);">
  <div class="eyebrow">Best Practices</div>
  <h2>Governance Habits<br>That Keep Things Clean</h2>
  <p class="section-sub">Super Admins who use these habits report fewer support issues and cleaner data throughout the season.</p>
  <div style="display:grid; grid-template-columns:1fr 1fr; gap:40px;" class="fade-up">
    <div>
      <h3 style="font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:1.3rem; text-transform:uppercase; color:var(--navy); margin-bottom:16px;">Season Start</h3>
      <ul class="check-list">
        <li>Audit user accounts — deactivate old ones</li>
        <li>Verify school and league memberships</li>
        <li>Confirm sport configurations are current</li>
        <li>Review and update role permissions</li>
        <li>Run a test import before real data goes in</li>
      </ul>
    </div>
    <div>
      <h3 style="font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:1.3rem; text-transform:uppercase; color:var(--navy); margin-bottom:16px;">Ongoing</h3>
      <ul class="check-list">
        <li>Review inbox escalations weekly</li>
        <li>Check for orphaned game records monthly</li>
        <li>Export a full data snapshot before major changes</li>
        <li>Monitor permission changes made by league admins</li>
        <li>Run end-of-season reports before archiving</li>
      </ul>
    </div>
  </div>
</section>

<section>
  <div class="eyebrow">Access Summary</div>
  <h2>The Full Permission Set</h2>
  <div style="display:grid; grid-template-columns:repeat(3,1fr); gap:32px;" class="fade-up">
    <div class="role-card">
      <div class="role-label">Data</div>
      <h3>All Schools &amp; Leagues</h3>
      <p>Create, edit, and delete any school or league in the system. No scope restrictions — full platform visibility.</p>
    </div>
    <div class="role-card">
      <div class="role-label">Users</div>
      <h3>Full User Control</h3>
      <p>Manage every user account. Create, deactivate, reset passwords, and reassign roles across the entire organization.</p>
    </div>
    <div class="role-card">
      <div class="role-label">Permissions</div>
      <h3>Role Configuration</h3>
      <p>The only role that can configure what other roles can do. Toggle permissions on or off for League Admins, ADs, Coaches, and Viewers.</p>
    </div>
  </div>
</section>

<div class="cta-strip">
  <h2>Take Full Control<br>of Your Platform</h2>
  <p>See how system administrators use ScheduleRama to govern a multi-league athletic program without chaos.</p>
  <a href="/contact/" class="btn-white">Book a Demo</a>
</div>
</Base>

