---
import Base from '../../layouts/Base.astro';
const title = "Coaches";
const description = `Find opponents, send requests, and stay on top of your team's schedule without touching a spreadsheet.`;
---

<Base title={title} description={description} activeSection="solutions">
<style>

  .nav-item > a:hover, .nav-item.active > a { color:#fff; }
  .nav-item.has-drop > a::after { content:' ▾'; font-size:0.65rem; }
  
  
  .nav-dropdown a:last-child { border-bottom:none; }
  .nav-dropdown a:hover { background:rgba(232,95,23,0.1); color:#fff; padding-left:22px; }
  
  .nav-cta:hover { background:var(--orange-bright) !important; }

.page-hero::before {
    content:''; position:absolute; right:0; top:0; bottom:0; width:40%;
    background:linear-gradient(90deg, transparent, rgba(232,95,23,0.04));
    pointer-events:none;
  }
  .page-hero-eyebrow {
    display:inline-flex; align-items:center; gap:8px;
    font-family:'JetBrains Mono',monospace; font-size:0.65rem;
    letter-spacing:0.2em; text-transform:uppercase; color:var(--orange);
    margin-bottom:20px;
  }
  .page-hero-eyebrow::before { content:''; display:block; width:28px; height:2px; background:var(--orange); }
  .page-hero h1 {
    font-family:'Barlow Condensed',sans-serif; font-weight:900;
    font-size:clamp(3rem,6vw,5.5rem); line-height:0.92;
    letter-spacing:-0.01em; color:#fff; text-transform:uppercase;
    max-width:700px; margin-bottom:22px;
  }
  .page-hero h1 span { color:var(--orange); }
  .page-hero p {
    font-size:1.1rem; line-height:1.7;
    color:rgba(255,255,255,0.55); max-width:560px;
  }
  .page-hero-actions { display:flex; gap:14px; margin-top:34px; flex-wrap:wrap; }

  /* ── SECTION COMMON ── */
  section { padding:88px 56px; }
  
  .eyebrow::after { content:''; display:block; height:1px; width:36px; background:var(--orange); }
  h2 {
    font-family:'Barlow Condensed',sans-serif; font-weight:800;
    font-size:clamp(2.4rem,4vw,3.8rem); line-height:0.95;
    letter-spacing:-0.01em; text-transform:uppercase;
    color:var(--navy); margin-bottom:14px;
  }

.btn-primary:hover { background:var(--orange-bright); transform:translateY(-2px); box-shadow:0 8px 24px rgba(232,95,23,0.4); }
  
  .btn-outline:hover { border-color:var(--orange); color:var(--orange); }
  .btn-navy {
    background:var(--navy); color:#fff; padding:14px 34px;
    border-radius:2px; font-size:0.9rem; font-weight:600;
    text-decoration:none; letter-spacing:0.06em; text-transform:uppercase;
    transition:all 0.2s; display:inline-block;
  }
  .btn-navy:hover { background:var(--navy-deep); transform:translateY(-2px); }

.feat-grid-2 { grid-template-columns:repeat(2,1fr); }
  
  .feat-tile:hover { background:#fff; }
  .feat-tile::after { content:''; position:absolute; bottom:0; left:0; right:0; height:3px; background:var(--orange); transform:scaleX(0); transform-origin:left; transition:transform 0.35s ease; }
  .feat-tile:hover::after { transform:scaleX(1); }
  .feat-tag { font-family:'JetBrains Mono',monospace; font-size:0.6rem; letter-spacing:0.15em; text-transform:uppercase; color:var(--orange); margin-bottom:12px; }
  .feat-tile h3 { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:1.4rem; letter-spacing:0.04em; text-transform:uppercase; color:var(--navy); margin-bottom:8px; }
  .feat-tile p { font-size:0.87rem; line-height:1.7; color:var(--ink-soft); }

  /* ── STEP LIST ── */
  .steps { display:flex; flex-direction:column; gap:0; }
  .step { display:grid; grid-template-columns:60px 1fr; gap:28px; padding:36px 0; border-bottom:1px solid var(--rule); align-items:start; }
  .step:last-child { border-bottom:none; }
  .step-num { font-family:'Barlow Condensed',sans-serif; font-weight:900; font-size:3rem; color:rgba(26,26,26,0.1); line-height:1; text-align:right; }
  .step h3 { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:1.5rem; letter-spacing:0.04em; text-transform:uppercase; color:var(--navy); margin-bottom:8px; }
  .step p { font-size:0.88rem; line-height:1.7; color:var(--ink-soft); }

.check-list li { display:flex; gap:12px; align-items:flex-start; font-size:0.9rem; line-height:1.6; color:var(--ink-mid); }
  .check-list li::before { content:'✓'; color:var(--teal); font-weight:700; flex-shrink:0; margin-top:1px; }

  /* ── ROLE CARDS ── */
  .role-card { background:#fff; border:1px solid var(--rule); border-radius:6px; padding:32px 28px; border-top:3px solid var(--orange); }
  .role-label { font-family:'JetBrains Mono',monospace; font-size:0.6rem; letter-spacing:0.15em; text-transform:uppercase; color:var(--orange); margin-bottom:8px; }
  .role-card h3 { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:1.5rem; letter-spacing:0.04em; text-transform:uppercase; color:var(--navy); margin-bottom:10px; }
  .role-card p { font-size:0.86rem; line-height:1.7; color:var(--ink-soft); }

.cta-strip h2 { color:#fff; margin-bottom:10px; }
  .cta-strip p { color:rgba(255,255,255,0.8); font-size:1rem; max-width:480px; margin:0 auto 28px; line-height:1.65; }
  
  .btn-white:hover { background:var(--navy-deep); color:#fff; transform:translateY(-2px); }

.footer-brand p { font-size:0.84rem; color:rgba(255,255,255,0.4); line-height:1.7; margin-top:12px; max-width:250px; }
  .footer-col h4 { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:0.9rem; letter-spacing:0.12em; text-transform:uppercase; color:var(--orange); margin-bottom:14px; }
  .footer-col ul { list-style:none; display:flex; flex-direction:column; gap:8px; }
  
  .footer-col a:hover { color:#fff; }
  
  .footer-bottom a { color:rgba(255,255,255,0.35); text-decoration:none; }
  .footer-bottom a:hover { color:var(--orange); }

  .fade-up.on { opacity:1; transform:none; }

.footer-brand p { font-size:0.84rem; color:rgba(255,255,255,0.4); line-height:1.7; margin-top:12px; max-width:250px; }
  .footer-col h4 { font-family:'Barlow Condensed',sans-serif; font-weight:700; font-size:0.9rem; letter-spacing:0.12em; text-transform:uppercase; color:var(--orange); margin-bottom:14px; }
  .footer-col ul { list-style:none; display:flex; flex-direction:column; gap:8px; }
  
  .footer-col a:hover { color:#fff; }
  
  .footer-bottom a { color:rgba(255,255,255,0.35); text-decoration:none; }
  .footer-bottom a:hover { color:var(--orange); }
</style>
<div class="page-hero">
  <div class="page-hero-eyebrow">Solutions</div>
  <h1>Built for <span>Coaches</span></h1>
  <p>Find opponents, send requests, and stay on top of your team's schedule — without touching a spreadsheet or making a single cold call.</p>
  <div class="page-hero-actions">
    <a href="/contact/" class="btn-primary">Book a Demo</a>
    <a href="/product/game-finder/" class="btn-outline">See Game Finder</a>
  </div>
</div>

<section>
  <div class="eyebrow">Your Tools</div>
  <h2>Everything Coaches<br>Need to Fill a Schedule</h2>
  <p class="section-sub">ScheduleRama gives coaches a focused, team-specific view — so you spend less time coordinating and more time coaching.</p>
  <div class="feat-grid fade-up">
    <div class="feat-tile">
      <div class="feat-tag">Opponent Discovery</div>
      <h3>Game Finder</h3>
      <p>Search available opponents by sport, level, and date. See who's open, review their schedule, and send a request — all without leaving the app.</p>
    </div>
    <div class="feat-tile">
      <div class="feat-tag">Communication</div>
      <h3>Requests Inbox</h3>
      <p>All your game requests in one thread-based inbox. See request status, message the other coach or AD, and track every decision with a full audit trail. New requests and replies arrive live — no refresh needed.</p>
    </div>
    <div class="feat-tile">
      <div class="feat-tag">Realtime</div>
      <h3>Direct Chat &amp; Live Updates</h3>
      <p>Message ADs and league admins directly from the slide-out chat drawer. Online presence shows who's connected right now. Toast notifications alert you to new requests and replies the moment they arrive.</p>
    </div>
    <div class="feat-tile">
      <div class="feat-tag">Your Season</div>
      <h3>Team Schedule View</h3>
      <p>A clean calendar view filtered to your team. See confirmed games, pending requests, and open dates at a glance. No noise from other sports or teams.</p>
    </div>
    <div class="feat-tile">
      <div class="feat-tag">Readiness</div>
      <h3>Checklist Tracking</h3>
      <p>Stay on top of pre-game checklist items for each contest. Know what's complete and what still needs attention — before it becomes a problem.</p>
    </div>
    <div class="feat-tile">
      <div class="feat-tag">Season Planning</div>
      <h3>Group Scheduler</h3>
      <p>View the Group Scheduler to see how league matchups were structured. Understand balance, home/away splits, and your full slate at a glance.</p>
    </div>
    <div class="feat-tile">
      <div class="feat-tag">Reporting</div>
      <h3>Export &amp; Print</h3>
      <p>Print your team's schedule for the locker room, export it for the school website, or share a CSV with parents and boosters in seconds.</p>
    </div>
  </div>
</section>

<section style="background:var(--navy); padding:88px 56px;">
  <div style="display:grid; grid-template-columns:1fr 1fr; gap:80px; align-items:center;">
    <div>
      <div class="eyebrow" style="color:var(--orange);">How It Works</div>
      <h2 style="color:#fff;">From Open Date<br>to Confirmed Game</h2>
      <p style="color:rgba(255,255,255,0.55); font-size:1rem; line-height:1.7; margin-bottom:32px;">The old way: call around, send emails, wait days for replies. The ScheduleRama way: search, request, confirm — all in one place.</p>
      <a href="/product/game-finder/" class="btn-primary">Explore Game Finder</a>
    </div>
    <div class="steps fade-up" style="border-top:1px solid rgba(255,255,255,0.08);">
      <div class="step" style="border-color:rgba(255,255,255,0.08);">
        <div class="step-num" style="color:rgba(255,255,255,0.1);">01</div>
        <div>
          <h3 style="color:#fff;">Find Open Opponents</h3>
          <p style="color:rgba(255,255,255,0.5);">Use Game Finder to search by sport, level, and your open dates. See who's available in real time.</p>
        </div>
      </div>
      <div class="step" style="border-color:rgba(255,255,255,0.08);">
        <div class="step-num" style="color:rgba(255,255,255,0.1);">02</div>
        <div>
          <h3 style="color:#fff;">Send a Request</h3>
          <p style="color:rgba(255,255,255,0.5);">Select the date and game details, add a message, and send. The other team's AD or coach gets notified immediately.</p>
        </div>
      </div>
      <div class="step" style="border-color:rgba(255,255,255,0.08);">
        <div class="step-num" style="color:rgba(255,255,255,0.1);">03</div>
        <div>
          <h3 style="color:#fff;">Track in Inbox</h3>
          <p style="color:rgba(255,255,255,0.5);">Follow the thread, respond to questions, and get notified the moment the game is confirmed or declined.</p>
        </div>
      </div>
      <div class="step" style="border-color:rgba(255,255,255,0.08); border-bottom:none;">
        <div class="step-num" style="color:rgba(255,255,255,0.1);">04</div>
        <div>
          <h3 style="color:#fff;">Game Appears on Calendar</h3>
          <p style="color:rgba(255,255,255,0.5);">Once confirmed, the game is on your schedule instantly. No double-entry, no manual updates.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="cta-strip">
  <h2>Fill Your Schedule<br>Faster</h2>
  <p>See how coaches use ScheduleRama to spend less time on logistics and more time on the field.</p>
  <a href="/contact/" class="btn-white">Book a Demo</a>
</div>
</Base>

